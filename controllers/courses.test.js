/*
import { it, expect } from "vitest";
import { addCourseToCart } from "./courses.js";

it("should add course to cart", async () => {
  // Arrange
  const request = {
    body: {
      courseId: `123`,
      selected_round: `1234`,
    },
  };

  const result = async () => {
    return await addCourseToCart(request, {}, (something) => {
      console.log(something);
    });
  };

  expect(await result()).toThrow();
});
*/
