<%- include("../includes/top_part.ejs"); %>
<link rel="stylesheet" href="/css/auth/login.css"/>
</head>
<body>
<%- include("../includes/navigation.ejs"); %>

<section class="login-form">
  <% if (errorMessage) { %>
    <div class="error-container">
      <%- errorMessage %>
    </div>
  <% } %>
  <div class="container content-container">
    <h2>Login</h2>
    <div class="header-dash-line"></div>
    <form action="/login" method="POST" novalidate>
      <div class="form-floating my-3">
        <input type="email" name="email" id="email" placeholder="Your email"
               class="form-control <%= validationErrors.email ? "is-invalid" : "" %>"
               value="<%= user.email %>"/>
        <label for="email">Your Email</label>
      </div>
      <div class="form-floating my-3">
        <input type="password" name="password" id="password" placeholder="Your password"
               class="form-control <%= validationErrors.password ? "is-invalid" : "" %>"
               value="<%= user.password %>"/>
        <label for="password">Your Password</label>
      </div>
      <div class="login-options">
        <a href="/forget_password" class="reset-link">Forget Password</a>
        <a href="/register" class="reset-link register-link">Do not have an account</a>
      </div>
      <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
      <button class="btn">
        Login
      </button>
    </form>
  </div>
</section>

<%- include("../includes/footer.ejs"); %>
<%- include("../includes/bottom_part.ejs"); %>
